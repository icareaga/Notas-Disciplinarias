export const environment = {
    production: true,
    /**
     * En producción NO se usa proxy del dev-server.
     * Aquí se define la URL real del backend (IIS/Nginx/servidor corporativo).
     */
    apiUrl: "https://mirh.megacorp.com.mx:84/api"
};